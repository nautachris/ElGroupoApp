@using ElGroupo.Web.Models.Activities
@model CreateActivityGroupModel

@functions{
    public string GenerateId(string prefix, object val)
    {
        return prefix + val.ToString();

    }


}

<section class="create-activity-group" style="text-transform:uppercase; text-align:center; padding:20px;">
    <h2>Create New Activity Group</h2>
    <input type="hidden" asp-for="MakePublic" />
    <div class="row">
        <label style="font-size:20px;" for="txtGroupName">Group Name:</label>
        <div class="width-100">
            <input type="text" style="width:75%;" id="txtGroupName" placeholder="Enter Group Name..." />
        </div>
        <div class="row hide-if-create-group-only" style="font-size:0.7em; margin-top:10px;">
            <a id="aEditTitle">Add Unique Title</a>
        </div>
    </div>

    @if (Model.MakePublic)
    {
        <div class="row" style="margin-top:25px;">
            <label>Share With</label>
            @foreach (var group in Model.AvailableGroups)
            {
                <div class="width-100">
                    @if (Model.SharedGroupIds.Contains(group.Id))
                    {
                        <input id="@GenerateId("rbShareGroup", group.Id)" type="checkbox" checked data-group-id="@group.Id" />

                    }
                    else
                    {
                        <input id="@GenerateId("rbShareGroup", group.Id)" type="checkbox" data-group-id="@group.Id" />

                    }

                    <label for="@GenerateId("rbShareGroup", group.Id)" style="margin-left:20px; cursor:pointer;">@group.Description</label>
                </div>

            }
        </div>

    }
    <div class="row uppercase group-save-options">
        <button id="btnSaveAsTile" class="btn">Save As Tile</button>
    </div>
    <div class="row uppercase group-save-options">
        <button id="btnContinueToLog" class="btn btn-primary">Continue to Log</button>
    </div>

    @*<div class="row hide-if-create-group-only" id="divCreditTypes" style="margin-top:25px;">
            <label>Credit Types</label>
            @foreach (var creditType in Model.CreditTypes)
            {
                <div class="width-100">
                    <input type="checkbox" id="@GenerateId("rbCreditType", creditType.Id)" data-credit-type-id="@creditType.Id" />
                    <label style="cursor:pointer;margin-left:20px;" for="@GenerateId("rbCreditType", creditType.Id)">@creditType.Description</label>
                </div>
            }
        </div>
        <div class="row hide-if-create-group-only" id="divCreditCategories" style="display:none;">
            <label>Credit Categories</label>
            @foreach (var creditCategory in Model.CreditCategories)
            {
                <div style="display:none; width:75%;" class="row line-after" data-credit-type-id="@creditCategory.CreditTypeId" data-credit-category-id="@creditCategory.Id">
                    <div class="col span-1-of-8"><input type="checkbox" id="@GenerateId("rbCreditCategory", creditCategory.Id)" /></div>
                    <div class="col span-5-of-8" style="text-align:left;"><label style="cursor:pointer;" for="@GenerateId("rbCreditCategory", creditCategory.Id)">@creditCategory.Description</label></div>
                    <div class="col span-1-of-8"><input type="number" style="width:50px;" min="0" max="24" value="0" /></div>
                    <div class="col span-1-of-8" style="text-align:left;"><span>Credits</span></div>




                </div>

            }
        </div>
        <div class="row hide-if-create-group-only" id="divAttendenceType">
            <label>Presenting?</label>
            <div class="width-100">
                <input type="radio" name="group" checked id="rbPresentingNo" />
                <label style="cursor:pointer;margin-left:20px;" for="rbPresentingNo">No</label>
            </div>
            <div class="width-100">
                <input type="radio" name="group" checked id="rbPresentingYes" />
                <label style="cursor:pointer;margin-left:20px;" for="rbPresentingYes">Yes</label>
            </div>
        </div>
        <div class="row hide-if-create-group-only">
            <label>Start Time</label>
            <div class="row centered" style="width:75%;">

                <div class="col span-3-of-6">
                    <input asp-for="StartTime" type="text" class="width-100" placeholder="Enter Event Start Date...">
                </div>
                <div class="col span-1-of-6">
                    <select id="selStartHour">
                        <option value="12" selected>12</option>
                        @for (int hour = 1; hour <= 11; hour++)
                        {
                            <option value="@hour">@hour.ToString()</option>
                        }
                    </select>
                </div>
                <div class="col span-1-of-6">
                    <select id="selStartMin">
                        @for (var min = 0; min < 60; min++)
                        {
                            @if (min < 10)
                            {
                                <option value="@min">@("0" + min.ToString())</option>

                            }
                            else
                            {
                                <option value="@min">@min.ToString()</option>
                            }

                        }
                    </select>
                </div>
                <div class="col span-1-of-6">
                    <select id="selStartAMPM">
                        <option value="AM">AM</option>
                        <option value="PM">PM</option>
                    </select>
                </div>
            </div>
        </div>
        <div class="row hide-if-create-group-only">
            <label>End Time</label>
            <div class="row centered" style="width:75%;">

                <div class="col span-3-of-6">
                    <input asp-for="EndTime" type="text" class="width-100" placeholder="Enter Event Start Date...">
                </div>
                <div class="col span-1-of-6">
                    <select id="selEndHour">
                        <option value="12" selected>12</option>
                        @for (int hour = 1; hour <= 11; hour++)
                        {
                            <option value="@hour">@hour.ToString()</option>
                        }
                    </select>
                </div>
                <div class="col span-1-of-6">
                    <select id="selEndMin">
                        @for (var min = 0; min < 60; min++)
                        {
                            @if (min < 10)
                            {
                                <option value="@min">@("0" + min.ToString())</option>

                            }
                            else
                            {
                                <option value="@min">@min.ToString()</option>
                            }

                        }
                    </select>
                </div>
                <div class="col span-1-of-6">
                    <select id="selEndAMPM">
                        <option value="AM">AM</option>
                        <option value="PM">PM</option>
                    </select>
                </div>
            </div>
        </div>

        <div class="row hide-if-create-group-only">
            <label for="txtDescription">Activity Description</label>
            <div class="width-100">
                <input style="width:75%;" type="text" id="txtDescription" />
            </div>

        </div>
        <div class="row hide-if-create-group-only">
            <label for="txtDescription">Activity Location</label>
            <div class="width-100">
                <input style="width:75%;" type="text" id="txtLocation" />
            </div>

        </div>
        <div class="row hide-if-create-group-only">
            <label>Documentation</label>
            <div style="display:none;" id="divRowTemplate">
                <div style="display: table-row; word-wrap:break-word; vertical-align:middle; border-bottom:1px solid white;; border-left:1px solid white; border-right:1px solid white;">
                    <div style="display: table-cell; vertical-align:middle;"></div>
                    <div style="display: table-cell; vertical-align:middle;"><input placeholder="Enter a File Description..." style="width:100%;" type="text" /></div>
                    <div style="display: table-cell; vertical-align:middle;"><img style="height:40px; width:40px; cursor:pointer;" src="~/content/images/delete.png" /></div>
                </div>
            </div>


            <div id="divDocumentTable" style="display:none;" class="row centered">
                <div id="divTable" style="display: table; margin-left:5%; margin-right:5%; width:90%; border-collapse:collapse;">
                    <header style="display: table-row;">
                        <div style="display: table-cell; width:30%; border:1px solid white;">File Name</div>
                        <div style="display: table-cell; width:60%; border:1px solid white;">Description</div>
                        <div style="display: table-cell; width:10%; border:1px solid white;"></div>
                    </header>
                </div>
            </div>
            <div class="row" style="width:75%; margin-top:25px;">
                <input style="display:none;" type="file" multiple id="iptFile" />
                <button id="btnAddDocuments" class="btn btn-primary">Add Document(s)</button>
            </div>
        </div>*@



    @Html.Partial("_CreateActivity", Model)


    <div class="row hide-if-create-group-only">
        @*<a class="btn btn-primary" asp-controller="Activities" asp-action="Dashboard">Return to Dashboard</a>*@
        <button id="btnCreate" class="btn btn-primary">Create Activity Group</button>
    </div>
    <div class="row centered hide-if-create-group-only" style="margin-top:0px;">
        <div class="col" style="text-align:center; font-size:0.7em; width:100%;">
            <a asp-controller="Activities" asp-action="Dashboard">Cancel</a>
        </div>
    </div>
</section>
@section Scripts{
    <script type="text/javascript" src="~/content/scripts/activities/create-activity-group.js"></script>

}